{
  "name": "spotlib-web-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser demo of spotlib (wasm) connecting to the live Spot network",
  "scripts": {
    "build:wasm": "wasm-pack build ../spot-web --target web --out-dir ../web/src/pkg --release && node scripts/patch-rng.mjs src/pkg/spot_web.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
